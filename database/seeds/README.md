Seeds folder

- Place initial data files here. For CSV-based source (diabetes), use the existing CSV in the project root or copy it here.
- Example: dinkes-od_17448_...csv (already present at project root)

To load seeds, run the project's database setup script (`setup_database.py`) or custom loader.
